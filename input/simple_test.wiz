## Simple test for parser
## Basic arithmetic and function

spell multiply(x: essence, y: essence) -> essence {
    return x * y;
}

ritual main() {
    essence result = multiply(5, 3);
    manifest(result);
}
